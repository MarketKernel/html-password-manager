# Dependencies
node_modules/

# Build output (build/password-manager.html is generated by `npm run build`)
build/

# Reference sources and local notes, kept locally only
keeweb-master/
docs/Task.md

# Real password databases and key files — never commit them.
# tools/fixtures/ holds the sample database the tests open.
*.kdbx
!/tools/fixtures/*.kdbx
*.kdbx.bak
*.kdbx.tmp
*.key
*.keyx

# Screenshots from `npm run test:browser -- --shots <dir>`
shots/
screenshots/

# Logs and caches
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/
*.tsbuildinfo

# Editors and IDEs
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.swp

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini

# Local environment
.env
.env.*
!.env.example
